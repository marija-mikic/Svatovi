﻿@{
    ViewData["Title"] = "<3";
}
@model ImagessModel


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Slider CSS</title>

    <link rel="stylesheet" href="~/css/slide.css">
</head>
@* <body>
 <div class="slider">
    <figure> 
         @for (int i = 0; i < Model.GalleryModels.Count(); i++)
            {
                  <div class="slide">                        
                         <img src="@Model.GalleryModels[i].URL" />
                   
                         <div class="coment">
                            <p>@Model.Coment</p>
                         </div>  
                  </div>                    
                
            }
     </figure>
       
 </div>

</body> *@

<div class="card-img">
    @for (int i = 0; i < Model.GalleryModels.Count(); i++)
    {
       @*  <div class="img-card"> *@
            <div class="img-card-1">
                <img src="@Model.GalleryModels[i].URL" />
             </div>       
                         
       @*  </div> *@
        
    }
    
</div>
<div class="card-content-img">
    <p>@Model.Coment  </p>

</div>

 