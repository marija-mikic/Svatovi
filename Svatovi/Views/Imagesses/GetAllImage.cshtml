@{
    ViewData["Title"] = "GetAllImage";
}
@model IEnumerable< ImagessModel >



<<div class="container">

    <h3 class="display-4">Image details</h3>
    
    <div class="row">
        @foreach (var image in Model){
            @* <div class="col-md-6">
                <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                    <ol class="carousel-indicators">

                        @for (int i = 0; i < image.GalleryModels.Count(); i++)
                        {
                            <li data-target="#carouselExampleIndicators" data-slide-to="@i" class="@(i==0 ? "active" : "") "></li>
                        }
                    </ol>
                    <div class="carousel-inner">
                        @for (int i = 0; i < image.GalleryModels.Count(); i++)
                        {
                            <div class="@(i==0 ? "carousel-item active" : "carousel-item") ">
                                <img class="d-block w-100" src="@image.GalleryModels[i].URL">
                            </div>
                        }

                    </div>
                    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                   
                </div>
            </div> *@
            <div class="card" style="width: 18rem;">
                <img src="@image.GalleryModels[i].URL" class="card-img-top"  >
                <div class="card-body">
                    <p class="card-text">@image.Coment</p>
                </div>
            </div>
        }

    </div>
</div>